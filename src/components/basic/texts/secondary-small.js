import React from 'react';
import {Text} from 'react-native';
import style from './style';

const SecondarySmall = ({text}) => {
  return <Text style={style.secondary_small}>{text}</Text>;
};

export default SecondarySmall;
